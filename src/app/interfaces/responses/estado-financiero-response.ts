export interface EstadoFinancieroResponse {
  cuenta: string;
  nombreCuenta: string;
  nivel: number;
  sum1: number | null;
  sum2: number | null;
  sum3: number | null;
  sum4: number | null;
  sum5: number | null;
  orden: number;
}